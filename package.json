{
  "name": "ipos",
  "version": "1.0.0",
  "description": "A golfing language for string processing",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run compile && npm test",
    "clean-dist": "rimraf dist",
    "compile": "npm run clean-dist && babel src --out-dir dist",
    "test": "mocha ./test --compilers js:babel-core/register --require ./test/helper.js",
    "docs": "babel-node tools/generateCommandDocs.js"
  },
  "author": "DenkerAffe",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-0": "^6.22.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "rimraf": "^2.5.4"
  }
}
